# 后端监听端口
APP_PORT=8080

# system 会调用 wg/wg-quick；本地开发可改为 file-only，仅保存配置文件
APP_TUNNEL_MODE=system

# 生产环境通常保留默认路径；本地开发请改到隔离目录
APP_WIREGUARD_DIRECTORY=/etc/wireguard
APP_AUTHENTICATION_FILE=/etc/wireguard-panel/auth.json
